# assets/images

Place raster images here (portfolio photos, project thumbnails, og-cover, etc.).
Add files (e.g. `img home-social-image.png`, `og-cover.png`) before deploying.
The site loads them via relative paths — no build step required.

Recommended formats: AVIF or WebP for photos, PNG for logos/icons with transparency.